install anaconda and python2 if they are not already installed
instructions on how to install anaconda here 
https://docs.anaconda.com/anaconda/install/

navigate to the doc directory in the terminal

run "conda env create --name <name> --file environment.yml" without the quotes and a name specified instead of <name>

run "python2.7 demo.py"


